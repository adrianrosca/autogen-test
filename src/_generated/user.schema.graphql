type User {
  name: ID!
}

type Query {
  users: [User!]!
  user(id: ID!): User
}

type Mutation {
  createUser(name: ID!): User!
  updateUser(id: ID!, name: ID): User
  deleteUser(id: ID!): Boolean!
}